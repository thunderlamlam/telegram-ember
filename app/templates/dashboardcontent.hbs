<div class=topdivider>
</div>
  <div class=dcontent>
    <p>{{textarea value=postBody cols="48" rows="3"}}</p> 
    <p>{{txtCount}} {{characters}} remaining</p>
      <button type="submit" class="Publish-button" {{action 'publish'}}>Publish</button>
    
 </div>

 <div class=topdivider2>

  <div class=dcontent2>
    Latest posts
  </div>	
  </div>

  <div class=dcontent3>
    {{#each dcpost in controller}}
      <div class=dcontent5>
        <p><img {{bind-attr src=dcpost.author.profileImage}} height="40" width="40"> 
        {{dcpost.author.name}} </p>
        {{dcpost.body}}
        <p> 20 minutes ago <button type="submit" class="Remove-button" {{action 'remove'}}>Delete</button></p>
      </div>
    {{/each}}
  </div>


